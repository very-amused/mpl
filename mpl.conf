bind p = play_toggle();
bind q = quit();
bind m = show_metadata();
bind , = seek_snap(-1000);
bind . = seek_snap(1000);
bind < = seek_snap(-5000);
bind > = seek_snap(5000);
bind : = shell_open();
shbind Escape = shell_close();
shbind Up = shell_history_prev();
shbind Down = shell_history_next();

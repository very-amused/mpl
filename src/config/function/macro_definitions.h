#pragma once

/* These macros can be bound to keys and called in mpl.conf.
 * They mutate MPL's configuration, and cause MPL to immediately reflect the changes made */

void include_default_keybinds(void * _);
